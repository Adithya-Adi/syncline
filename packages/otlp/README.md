# otlp

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build otlp` to build the library.

## Running unit tests

Run `nx test otlp` to execute the unit tests via [Vitest](https://vitest.dev/).
