packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 11 gates postinstall scripts. Add entries here as deps that need them arrive
# (prisma, esbuild) — `pnpm approve-builds` writes them for you.
allowBuilds:
  '@parcel/watcher': true
  '@swc/core': true
  less: true
  nx: true
  unrs-resolver: true
